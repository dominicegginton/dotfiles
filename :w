{ config, lib, hostname, ... }:

{
  config = {
    services = {
      zabbixAgent = {
        enable = true;
        server = "ghost-gs60";
        settings."Hostname" = hostname;
      };

      zabbixWeb = lib.mkIf (config.services.zabbixAgent.enable) {
      };
    };
  };
}
