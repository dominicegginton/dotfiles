[project]
name = "dominicegginton-personal"
revision = "1.0"
require_reason = false

[providers]
secrets = "gcsm://dominicegginton-personal"

[profiles.default]
TF_VAR_tailscale_api_key = { description = "Tailscale API key", providers = ["secrets"] }
TF_VAR_tailscale_tailnet = { description = "Tailscale Tailnet", providers = ["secrets"] }
TF_VAR_nextdns_api_token = { description = "Nextdns", providers = ["secrets"] }
TF_VAR_nextdns_profile_ribble = { description = "NextDNS Profile ID for Ribble", providers = ["secrets"] }
TF_VAR_nextdns_profile_quandon = { description = "NextDNS Profile ID for Quandon", providers = ["secrets"] }
